[pytest]
testpaths = tests
pythonpath = .
addopts = -q
markers =
    integracao: roda contra os arquivos reais do Sicor em dados_locais/ (lento, minutos)
    rede: libera chamada de rede real (a fixture autouse bloqueia por padrão)
filterwarnings =
    error::DeprecationWarning:app.*
